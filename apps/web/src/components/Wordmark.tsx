import React from 'react';
import { useAppStore } from '../store/useAppStore';

const Wordmark = () => {
  return (
    <h1 className="font-display text-2xl lowercase font-normal text-text-primary tracking-tight">
      kairos
    </h1>
  );
};

export default Wordmark;
